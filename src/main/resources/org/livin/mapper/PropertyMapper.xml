<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.livin.mapper.PropertyMapper">

    <!-- 거리 기반 매물 조회 쿼리 -->
    <select id="findNearbyProperties" resultType="org.livin.dto.PropertyDTO">
        SELECT
            property_id as propertyId,
            property_name as propertyName,
            address,
            property_type as propertyType,
            monthly_rent as monthlyRent,
            deposit,
            area,
            floor,
            total_floors as totalFloors,
            image_url as imageUrl,
            latitude,
            longitude,
            description,
            has_elevator as hasElevator,
            has_parking as hasParking,
            available_date as availableDate,
            (
                6371 * acos(
                        cos(radians(#{lat})) * cos(radians(latitude)) *
                        cos(radians(longitude) - radians(#{lng})) +
                        sin(radians(#{lat})) * sin(radians(latitude))
                       )
                ) AS distance
        FROM properties
        WHERE status = 'AVAILABLE'
        HAVING distance &lt;= #{radius}
        ORDER BY distance ASC
            LIMIT #{limit}
    </select>

</mapper>