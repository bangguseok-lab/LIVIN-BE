<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.livin.property.mapper.FavoritePageMapper"> <delete id="deleteFavoriteProperty">
    DELETE FROM FavoriteProperty
    WHERE user_id = #{userId} AND property_id = #{propertyId}
</delete>

    <select id="isPropertyFavorite" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM FavoriteProperty
        WHERE user_id = #{userId} AND property_id = #{propertyId}
    </select>

</mapper>