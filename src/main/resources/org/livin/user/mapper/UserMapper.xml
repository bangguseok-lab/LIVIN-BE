<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.livin.user.mapper.UserMapper">


    <resultMap id="userResultMap" type="org.livin.user.entity.User">
        <id property="userId" column="user_id"/>
        <result property="provider" column="provider"/>
        <result property="providerId" column="provider_id"/>
        <result property="name" column="name"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
        <result property="birthDate" column="birth_date"/>
        <result property="phone" column="phone"/>
        <result property="nickname" column="nickname"/>
        <result property="profileImage" column="profile_image"/>
    </resultMap>

    <delete id="deleteByProviderAndProviderId">
        DELETE
        FROM user
        WHERE provider_id = #{providerId}
    </delete>

    <select id="findByProviderAndProviderId" resultMap="userResultMap">
        SELECT *
        FROM User
        WHERE provider = #{provider}
          AND provider_id = #{providerId}
    </select>

    <insert id="insertUser" parameterType="org.livin.user.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO User (provider, provider_id, name, role, created_at, birth_date, phone, nickname, profile_image)
        VALUES (#{provider}, #{providerId}, #{name}, #{role}, now(), #{birthDate},
                #{phone}, #{nickname}, #{profileImage})
    </insert>


    <select id="findNicknameByUserId" resultType="UserVO" parameterType="String">
        SELECT nickname
        FROM user
        WHERE provider_Id = #{providerId}
    </select>


    <select id="findUserIdByProviderId" parameterType="string" resultType="long">
        SELECT user_id
        FROM user
        WHERE provider_id = #{providerId}
    </select>

    <select id="findByProviderId" resultType="org.livin.user.entity.UserVO">
        SELECT *
        FROM user
        WHERE user_id = #{userId}
    </select>

    <select id="findById" resultType="org.livin.user.entity.UserVO">
        SELECT *
        FROM users
        WHERE id = #{id}
    </select>

</mapper>